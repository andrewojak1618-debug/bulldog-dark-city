/**
 * Beschreibt die Social-Media-Symbole des Startmenüs.
 * Externe Links werden ergänzt, sobald die offiziellen Profile feststehen.
 * @type {ReadonlyArray<{
 *   textureKey: string,
 *   iconFile: string
 * }>}
 */
export const SOCIAL_ACTIONS = Object.freeze([
  Object.freeze({
    textureKey: "social-discord",
    iconFile: "menu/social/discord.png",
  }),
]);
