/** @type {ReadonlyArray<{ action: string, label: string, label: string }>} */
export const MENU_BUTTONS = Object.freeze([
  Object.freeze({
    action: "start",
    label: "START",
  }),
  Object.freeze({
    action: "options",
    label: "OPTIONEN",
  }),
  Object.freeze({
    action: "upgrades",
    label: "UPGRADES",
  }),
  Object.freeze({
    action: "extras",
    label: "EXTRAS",
  }),
]);
